"""Global variables for programs."""

# Holds the current longest length.
LONGEST_TERM = {'term': 1, 'length': 1}
# Number of terms in a sequence.
COUNT_TERM_LENGTH = 1
# Not an array, because will delete objects on the way with
# keys varying.
CACHING_DICT = {}
# Use the cache or not.
USE_CACHE = False
